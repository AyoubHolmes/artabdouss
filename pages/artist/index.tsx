import { HeroSection } from "components/Artist";
import Layout from "components/Layout";

const Artist = () => {
  return (
    <Layout>
      <HeroSection />
    </Layout>
  );
};

export default Artist;
